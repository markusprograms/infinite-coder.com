# Production-only files
This directory contains files that are used on the **production server only**. These files are not included in the Docker image. 

## [.htaccess](./.htaccess)
The root .htaccess that **redirects all traffic to HTTPS and non-WWW**. 